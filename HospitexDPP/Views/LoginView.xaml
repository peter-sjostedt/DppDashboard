<UserControl x:Class="HospitexDPP.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
        <StackPanel Width="400" Margin="20">
            <Image Source="/DPP-2.png" Height="180" Stretch="Uniform"
                   HorizontalAlignment="Center" Margin="0,0,0,12" />
            <TextBlock Text="HospitexDPP" FontSize="26" FontWeight="Bold"
                       HorizontalAlignment="Center" Margin="0,0,0,24" />

            <TextBlock Text="Brand-nyckel (valfritt)" Margin="0,0,0,4" FontWeight="SemiBold" />
            <TextBox Text="{Binding BrandKey, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0,0,0,12" Padding="6,4" />

            <TextBlock Text="Supplier-nyckel (valfritt)" Margin="0,0,0,4" FontWeight="SemiBold" />
            <TextBox Text="{Binding SupplierKey, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0,0,0,16" Padding="6,4" />

            <CheckBox Content="Kom ihåg mig" IsChecked="{Binding RememberMe}"
                      Margin="0,0,0,16" />

            <Button Content="Anslut"
                    Command="{Binding LoginCommand}"
                    Padding="12,6"
                    Margin="0,0,0,12"
                    HorizontalAlignment="Left" />

            <TextBlock Text="{Binding StatusMessage}"
                       TextWrapping="Wrap">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Foreground" Value="#4CAF50" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding StatusIsError}" Value="True">
                                <Setter Property="Foreground" Value="Red" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding StatusMessage}" Value="">
                                <Setter Property="Visibility" Value="Collapsed" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </StackPanel>
    </Grid>
</UserControl>
