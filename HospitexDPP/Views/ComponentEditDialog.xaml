<Window x:Class="HospitexDPP.Views.ComponentEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:h="clr-namespace:HospitexDPP.Helpers"
        Title="{h:Loc Button_AddComponent}"
        Width="420" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource SurfaceBrush}">
    <StackPanel Margin="24,20">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" MinWidth="100" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Component -->
            <TextBlock Grid.Row="0" Grid.Column="0" Text="{h:Loc Column_Component}"
                       Style="{DynamicResource FormLabel}" TextAlignment="Right"
                       VerticalAlignment="Center" Margin="0,0,12,8" />
            <ComboBox Grid.Row="0" Grid.Column="1" x:Name="CmbComponent"
                      Margin="0,0,0,8" Padding="10,8" FontSize="13"
                      DisplayMemberPath="DisplayName" SelectedValuePath="Value" />

            <!-- Material -->
            <TextBlock Grid.Row="1" Grid.Column="0" Text="{h:Loc Column_Material}"
                       Style="{DynamicResource FormLabel}" TextAlignment="Right"
                       VerticalAlignment="Center" Margin="0,0,12,8" />
            <ComboBox Grid.Row="1" Grid.Column="1" x:Name="CmbMaterial"
                      Margin="0,0,0,8" Padding="10,8" FontSize="13"
                      DisplayMemberPath="DisplayName" SelectedValuePath="Value" />

            <!-- Content Name -->
            <TextBlock Grid.Row="2" Grid.Column="0" Text="{h:Loc Column_ContentName}"
                       Style="{DynamicResource FormLabel}" TextAlignment="Right"
                       VerticalAlignment="Center" Margin="0,0,12,8" />
            <TextBox Grid.Row="2" Grid.Column="1" x:Name="TxtContentName"
                     Style="{DynamicResource FormTextBox}" Margin="0,0,0,8" />

            <!-- Content Value (%) -->
            <TextBlock Grid.Row="3" Grid.Column="0" Text="{h:Loc Column_Percentage}"
                       Style="{DynamicResource FormLabel}" TextAlignment="Right"
                       VerticalAlignment="Center" Margin="0,0,12,8" />
            <TextBox Grid.Row="3" Grid.Column="1" x:Name="TxtContentValue"
                     Style="{DynamicResource FormTextBox}" Margin="0,0,0,8" />

            <!-- Content Source -->
            <TextBlock Grid.Row="4" Grid.Column="0" Text="{h:Loc Column_Source}"
                       Style="{DynamicResource FormLabel}" TextAlignment="Right"
                       VerticalAlignment="Center" Margin="0,0,12,8" />
            <TextBox Grid.Row="4" Grid.Column="1" x:Name="TxtContentSource"
                     Style="{DynamicResource FormTextBox}" Margin="0,0,0,8" />
        </Grid>

        <!-- Buttons -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="{h:Loc Button_Save}"
                    Style="{DynamicResource PrimaryButton}"
                    Click="BtnSave_Click" Margin="0,0,8,0"
                    IsDefault="True" />
            <Button Content="{h:Loc Button_Cancel}"
                    Style="{DynamicResource SecondaryButton}"
                    Click="BtnCancel_Click"
                    IsCancel="True" />
        </StackPanel>
    </StackPanel>
</Window>
