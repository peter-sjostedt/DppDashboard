<Window x:Class="HospitexDPP.SplashWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:h="clr-namespace:HospitexDPP.Helpers"
        WindowStyle="None" AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        Width="420" Height="340"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Border Background="White" CornerRadius="12"
            BorderBrush="#0D6EFD" BorderThickness="2"
            Padding="40,32">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" Opacity="0.25" ShadowDepth="4" />
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Logo -->
            <Image Source="/DPP-2.png" Height="120" Stretch="Uniform"
                   HorizontalAlignment="Center" Margin="0,0,0,16" />

            <!-- App name -->
            <TextBlock Grid.Row="1" Text="Hospitex DPP"
                       FontSize="24" FontWeight="Bold"
                       Foreground="#343A40"
                       HorizontalAlignment="Center" Margin="0,0,0,8" />

            <!-- Tagline -->
            <TextBlock Grid.Row="2" Text="{h:Loc Splash_Tagline}"
                       FontSize="13" Foreground="#6C757D"
                       HorizontalAlignment="Center" Margin="0,0,0,0" />

            <!-- Version -->
            <TextBlock Grid.Row="4" x:Name="VersionText"
                       FontSize="11" Foreground="#ADB5BD"
                       HorizontalAlignment="Center" />
        </Grid>
    </Border>
</Window>
