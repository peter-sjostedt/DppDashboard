<UserControl x:Class="DppDashboard.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
        <StackPanel Width="350" Margin="20">
            <TextBlock Text="Admin API Key" Margin="0,0,0,5" FontWeight="SemiBold" />
            <TextBox Text="{Binding AdminKey, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0,0,0,10" Padding="5" />
            <Button Content="Anslut"
                    Command="{Binding LoginCommand}"
                    Padding="10,5"
                    Margin="0,0,0,10"
                    HorizontalAlignment="Left" />
            <TextBlock Text="{Binding StatusMessage}"
                       TextWrapping="Wrap"
                       Foreground="Gray" />
        </StackPanel>
    </Grid>
</UserControl>
